jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wishlist
    username: postgres
    password: ${DB_PASSWORD:mysecretpassword}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
  application:
    name: wishlist
